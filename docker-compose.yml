version: "3.9"
services:
  mcp-server:
    build: .
    image: workout-planner:latest   
    container_name: workout-planner
    environment:
      - DB_PATH=/app/data/workouts.db
    volumes:
      - workouts-data:/app/data
    stdin_open: true
    tty: true

volumes:
  workouts-data:
